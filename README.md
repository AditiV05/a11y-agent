# a11y-agent

An agent that detects, fixes, and verifies web accessibility issues — using axe-core for deterministic detection and an LLM only for judgment (writing the actual fix).

🚧 Work in progress, built slice by slice.
